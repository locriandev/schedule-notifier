apiVersion: v1
kind: ConfigMap
metadata:
  name: weekly-schedule-app
  namespace: weekly-schedule-notifier
data:
  schedule_notifier.py: |
    # This will be populated with the actual script content
    # Use: kubectl create configmap weekly-schedule-app --from-file=schedule_notifier.py=./schedule_notifier.py --dry-run=client -o yaml > k8s/app-configmap.yaml
